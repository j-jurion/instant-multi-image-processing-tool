from pathlib import Path

import numpy as np


def save_image(image: np.ndarray, filename: str, output_directory: Path) -> None:
    print(f"Saving image '{filename}' to '{output_directory}'")
    pass
